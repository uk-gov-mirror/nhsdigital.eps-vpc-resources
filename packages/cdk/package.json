{
  "name": "cdk",
  "version": "0.1.0",
  "scripts": {
    "cdk-synth": "cdk synth  --output ../../cdk.out --quiet --app \"npm run tsx -- bin/${CDK_APP_NAME}.ts\"",
    "cdk-diff": "cdk diff ${CDK_STACK_NAME}  --output ../../cdk.out --app \"npm run tsx -- bin/${CDK_APP_NAME}.ts\"",
    "cdk-deploy": "cdk deploy ${CDK_STACK_NAME} --app \"npm run tsx -- bin/${CDK_APP_NAME}.ts\" --ci true --require-approval ${REQUIRE_APPROVAL}",
    "cdk-prepare-changeset": "cdk deploy ${CDK_STACK_NAME} --change-set-name ${CDK_CHANGE_SET_NAME} --method prepare-change-set --app \"npm run tsx -- bin/${CDK_APP_NAME}.ts\" --ci true --require-approval ${REQUIRE_APPROVAL}",
    "cdk-check-changeset": "tsx scripts/manageDestructiveChanges.ts",
    "lint": "eslint  --max-warnings 0 --fix --config ../../eslint.config.mjs .",
    "tsx": "tsx"
  },
  "dependencies": {
    "@nhsdigital/eps-cdk-constructs": "^1.7.0",
    "aws-cdk-lib": "^2.251.0",
    "cdk-nag": "^2.38.1",
    "constructs": "^10.6.0",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "aws-cdk": "^2.1118.2",
    "tsx": "^4.21.0"
  }
}
